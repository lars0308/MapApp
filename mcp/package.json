{
  "name": "mapforge-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server for MapForge – lets an AI create maps and figures in MapForge",
  "bin": { "mapforge-mcp": "server.mjs" },
  "scripts": {
    "start": "node server.mjs --no-stdio"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "ws": "^8.21.3"
  },
  "optionalDependencies": {
    "playwright-core": "^1.56.0"
  },
  "engines": { "node": ">=20" }
}
